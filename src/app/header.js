import { MeshBackgroundHeader, MeshBackgroundHero } from "@/components/MeshBackground";

export default function Header() {
    return (
        <MeshBackgroundHeader className='header'>
            <section>
                <div className="flex flex-col flex-1 items-center justify-center font-sans">
                </div>
            </section>
        </MeshBackgroundHeader>
    );
}